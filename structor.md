# create a bash-file, example bash_variable

#!/bin/bash
x="das ist definition eine variable, die darf ohne leerzeichen sein."
echo $x

# set chmod
    chmod 744 [filename]

# invoke / execute
   example ./Desktop/bash_variable
